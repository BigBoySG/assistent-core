[project]
name = "assistanc-core"
version = "0.1.0"
description = "Core package for personal assistant"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]